interface TrendRangeParams {
  from: number;
  to: number;
  interval: number;
}

interface RangeParams {
  from: number;
  to: number;
}

interface CommonParams {
  appKey?: string;
  release?: string;
  environment?: string;
}
